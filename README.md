 # Sun City / Funding Observatory

An interactive Vue dashboard for reviewing Sun City's grants, climate signals, service gaps, and funding outcomes.

## Run locally

```bash
npm install
npm run dev
```

## Build for Vercel

Vercel can deploy this Vite static site with the default settings:

- Build command: `npm run build`
- Output directory: `dist`
